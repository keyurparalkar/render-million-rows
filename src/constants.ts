export const DEFAULT_CELL_DIMS = {
	width: 150,
	height: 50,
};

export const DEFAULT_COLUMN_LENGTH = 12;

export const DEFAULT_HEADER_HEIGHT = 50;

export const DEFAULT_SLICE_THRESHOLD = 100;

export const DEFAULT_TABLE_CONTAINER_HEIGHT = 500;

export const DEFAULT_TABLE_CONTAINER_WIDTH = 1200;
